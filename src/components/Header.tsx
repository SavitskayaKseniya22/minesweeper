import React from 'react';
import { StyledHeader } from './styledComponents';

function Header() {
  return (
    <StyledHeader>
      <h2>Minesweeper</h2>
    </StyledHeader>
  );
}

export default Header;
